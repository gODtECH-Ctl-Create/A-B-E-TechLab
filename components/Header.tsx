"use client";
import Link from "next/link";
import { Menu, X } from "lucide-react";
import { useState } from "react";

export function Logo(){return <Link href="/" aria-label="ABE TechLab home" className="flex items-center gap-2"><span className="font-display text-xl font-bold tracking-[-.06em]">ABE</span><span className="text-sm font-medium tracking-[-.02em]">TechLab</span></Link>}

export function Header(){const [open,setOpen]=useState(false); const links=[['About','/about'],['Products','/products'],['Programs','/programs'],['Services','/services'],['Insights','/insights'],['Contact','/contact']]; return <header className="sticky top-0 z-50 border-b border-black/10 bg-[#f5f5f2]/90 backdrop-blur-md"><div className="container flex h-[72px] items-center justify-between"><Logo/><nav className="hidden items-center gap-6 lg:flex">{links.map(([label,href])=><Link key={href} href={href} className="text-sm text-black/65 transition hover:text-black">{label}</Link>)}</nav><Link href="/contact" className="btn-primary hidden px-5 py-3 text-sm font-semibold transition md:inline-flex">Start a project</Link><button aria-label={open?'Close menu':'Open menu'} onClick={()=>setOpen(!open)} className="md:hidden">{open?<X size={24}/>:<Menu size={24}/>}</button></div>{open&&<div className="border-t border-black/10 bg-[#f5f5f2] md:hidden"><nav className="container flex flex-col py-5">{links.map(([label,href])=><Link onClick={()=>setOpen(false)} key={href} href={href} className="border-b border-black/10 py-4 text-lg">{label}</Link>)}<Link onClick={()=>setOpen(false)} href="/contact" className="btn-primary mt-5 px-5 py-4 text-center font-semibold">Start a project</Link></nav></div>}</header>}
